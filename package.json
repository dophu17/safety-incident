{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "scripts": {
        "test": "echo \"No tests configured\""
    },
    "devDependencies": {
        "axios": "^1.11.0"
    }
}
